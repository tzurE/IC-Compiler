// While + if + break statement
// "break" statement should exit "while" when x = 444
// because it's the closest value to 396 that is divided by "111".
// Expected to print "444", since it's the closest

class B {


  static void main(string[] args) {

	int x = 11;
	string g = "good";
	string b = "bad";

	if (x > 0){
		if (x < 10){
			Library.print(b);
		}
		else{
			if (x%2 == 0){
				Library.print(b);
			}
			else {
				Library.print(g);
			}
		}
	}
	else{
		Library.print(b);
	}
  }
}
