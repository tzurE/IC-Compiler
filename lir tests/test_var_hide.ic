class A {
	int x;
	void f() {
		int x = 3;
		if (x > 0) {
			int x = 5;

			Library.print("Five: ");
			Library.print(Library.itos(x));
			Library.print(", ");

			if (x < 5)
				Library.print("ERROR!");

			x = x + 1;

			Library.print("Six: ");
			Library.print(Library.itos(x));
			Library.print(", ");
		}

		Library.print("Three: ");
		Library.print(Library.itos(x));
		Library.print(", ");
	}
	static void main(string[] args) {
		A a = new A();
		a.f();
	}
}